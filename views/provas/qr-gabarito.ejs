<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gabarito - <%= prova.titulo %></title>
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background: #f5f7fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .header {
            text-align: center;
            border-bottom: 2px solid #e1e5e9;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .header h1 {
            color: #667eea;
            margin: 0 0 10px 0;
            font-size: 2em;
        }

        .header p {
            color: #666;
            margin: 0;
            font-size: 1.1em;
        }

        .gabarito-info {
            background: #fff3cd;
            border: 2px solid #ffeaa7;
            padding: 15px;
            margin-bottom: 30px;
            border-radius: 8px;
        }

        .gabarito-info h3 {
            margin: 0 0 10px 0;
            color: #856404;
        }

        .gabarito-info p {
            margin: 0;
            color: #856404;
            font-weight: 600;
        }

        .questao-gabarito {
            background: #f8f9fa;
            border: 1px solid #e1e5e9;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 15px;
        }

        .questao-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            flex-wrap: wrap;
        }

        .questao-numero {
            font-weight: bold;
            color: #495057;
            font-size: 1.1em;
        }

        .questao-area {
            background: #667eea;
            color: white;
            padding: 4px 12px;
            border-radius: 15px;
            font-size: 0.9em;
        }

        .questao-enunciado {
            font-size: 1.1em;
            line-height: 1.6;
            margin-bottom: 15px;
            color: #212529;
        }

        .opcoes {
            list-style: none;
            padding: 0;
        }

        .opcao {
            background: white;
            border: 1px solid #e1e5e9;
            border-radius: 5px;
            padding: 12px 15px;
            margin-bottom: 8px;
            transition: all 0.2s;
        }

        .opcao.correta {
            background: #d4edda;
            border-color: #c3e6cb;
            font-weight: 600;
            border-left: 4px solid #28a745;
        }

        .resposta-aberta {
            background: #e8f5e8;
            border: 2px solid #28a745;
            border-radius: 8px;
            padding: 15px;
            margin-top: 10px;
        }

        .resposta-aberta h4 {
            margin: 0 0 10px 0;
            color: #155724;
        }

        .resposta-aberta p {
            margin: 0;
            color: #155724;
            font-style: italic;
        }

        .btn-voltar {
            display: inline-block;
            background: #667eea;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            margin-top: 30px;
            transition: transform 0.2s;
        }

        .btn-voltar:hover {
            transform: translateY(-2px);
            color: white;
            text-decoration: none;
        }

        .stats {
            background: #e9ecef;
            border-radius: 8px;
            padding: 15px;
            margin-bottom: 20px;
            text-align: center;
        }

        .stats h4 {
            margin: 0 0 10px 0;
            color: #495057;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
            gap: 10px;
            margin-top: 10px;
        }

        .stat-item {
            background: white;
            padding: 10px;
            border-radius: 5px;
            border: 1px solid #dee2e6;
        }

        .stat-value {
            font-size: 1.2em;
            font-weight: bold;
            color: #667eea;
        }

        .stat-label {
            font-size: 0.9em;
            color: #6c757d;
        }

        @media print {
            body { background: white !important; }
            .btn-voltar { display: none !important; }
            .container { box-shadow: none !important; border: 1px solid #ccc !important; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📋 Gabarito da Prova</h1>
            <p><%= prova.titulo %></p>
        </div>

        <div class="gabarito-info">
            <h3>📚 Informações da Prova</h3>
            <p>
                <strong>Disciplina:</strong> <%= prova.disciplina %> | 
                <strong>Turma:</strong> <%= prova.turma_nome %> | 
                <strong>Total de Questões:</strong> <%= questoes.length %>
            </p>
        </div>

        <div class="stats">
            <h4>📊 Estatísticas da Prova</h4>
            <div class="stats-grid">
                <div class="stat-item">
                    <div class="stat-value"><%= questoes.length %></div>
                    <div class="stat-label">Total Questões</div>
                </div>
                <div class="stat-item">
                    <div class="stat-value"><%= questoes.filter(q => q.tipo_questao === 'multipla_escolha').length %></div>
                    <div class="stat-label">Múltipla Escolha</div>
                </div>
                <div class="stat-item">
                    <div class="stat-value"><%= questoes.filter(q => q.tipo_questao === 'questao_aberta').length %></div>
                    <div class="stat-label">Questões Abertas</div>
                </div>
                <div class="stat-item">
                    <div class="stat-value"><%= [...new Set(questoes.map(q => q.area))].length %></div>
                    <div class="stat-label">Áreas Diferentes</div>
                </div>
            </div>
        </div>

        <div class="questoes">
            <% questoes.forEach((questao, index) => { %>
                <div class="questao-gabarito">
                    <div class="questao-header">
                        <span class="questao-numero">Questão <%= index + 1 %></span>
                        <span class="questao-area"><%= questao.area %></span>
                    </div>
                    
                    <div class="questao-enunciado">
                        <%= questao.enunciado %>
                    </div>
                    
                    <% if (questao.tipo_questao === 'questao_aberta') { %>
                        <div class="resposta-aberta">
                            <h4>✅ Resposta de Referência:</h4>
                            <p><%= JSON.parse(questao.opcoes)[0] %></p>
                        </div>
                    <% } else { %>
                        <ul class="opcoes">
                            <% JSON.parse(questao.opcoes).forEach((opcao, opcaoIndex) => { %>
                                <li class="opcao <%= opcaoIndex === questao.resposta_correta ? 'correta' : '' %>">
                                    <%= String.fromCharCode(65 + opcaoIndex) %>) <%= opcao %>
                                    <% if (opcaoIndex === questao.resposta_correta) { %>
                                        ✓ RESPOSTA CORRETA
                                    <% } %>
                                </li>
                            <% }); %>
                        </ul>
                    <% } %>
                </div>
            <% }); %>
        </div>

        <div style="text-align: center;">
            <a href="javascript:window.print()" class="btn-voltar">🖨️ Imprimir Gabarito</a>
            <a href="javascript:history.back()" class="btn-voltar">← Voltar</a>
        </div>
    </div>
</body>
</html>

